version: "3.8"

services:
  db:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER = admin
      - POSTGRES_PASSWORD = postgres123
      - POSTGRES_DB = vintage_instruments